import 'package:flutter/material.dart';

class ServiceProvidorDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dashboard'),
      ),
      body: Text('Admin Dashboard'),
    );
  }
}
